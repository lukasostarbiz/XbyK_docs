# Content retrieval
  * [ Copy page link ](documentation/developers-and-admins/development/content-retrieval#) | [Get HelpService ID](documentation/developers-and-admins/development/content-retrieval#)
Core MVC 5


[✖](documentation/developers-and-admins/development/content-retrieval# "Close page link panel") [Copy to clipboard](documentation/developers-and-admins/development/content-retrieval#)
This section describes how to retrieve data from Xperience. You can work with the following system objects:
  * [Pages](documentation/developers-and-admins/development/content-retrieval/retrieve-page-content)
  * [Content items](documentation/developers-and-admins/development/content-retrieval/retrieve-content-items)
  * [Form records](documentation/developers-and-admins/development/content-retrieval#retrieve-form-data)
  * [Media library files](documentation/developers-and-admins/development/content-retrieval/retrieve-content-from-media-libraries)
  * [General objects managed by the system](documentation/developers-and-admins/development/content-retrieval#retrieve-general-object-data)
  * [Retrieve content from headless channels using GraphQL](documentation/developers-and-admins/development/content-retrieval/retrieve-headless-content)


## Retrieve form data
When retrieving forms data, use `BizFormItemProvider` and [generated](documentation/developers-and-admins/api/generate-code-files-for-system-objects) _< FormCodeName>Item_ classes. `BizFormItemProvider` exposes two methods to facilitate form data retrieval:
  * `GetItem<TType>`–returns a record of the specified type and ID.
  * `GetItems<TType>` – returns all records of the specified type using the [ObjectQuery API](documentation/developers-and-admins/api/objectquery-api).


The following example demonstrates how to retrieve individual submissions from a ‘ _Contact Us_ ’ form. The code uses a generated form data class (`ContactUsItem`) to work with individual submitted entries.
C#
Copy
```
// Gets all form items of the given type
IEnumerable<ContactUsItem> items = BizFormItemProvider.GetItems<ContactUsItem>();

// Gets a single form item with the given type and ID
ContactUsItem item = BizFormItemProvider.GetItem<ContactUsItem>(1);

// Programatically assigns and saves a value for the form record's 'CustomField' input
object customFieldValue = "CustomValue";
item.SetValue(nameof(item.Fields.CustomField), customFieldValue);
item.SubmitChanges(false);
```

## Retrieve general object data
To retrieve general objects managed by the system, use the corresponding _< Class>Info_ classes (e.g., `UserInfo`) and the `I<Class>InfoProvider` interface to retrieve the data. The query generated by the `Get` method of the interface can be parameterized using ObjectQuery syntax, see [ObjectQuery API](documentation/developers-and-admins/api/objectquery-api) and [Database table API](documentation/developers-and-admins/api/database-table-api) for more information.
The following example shows how to get user objects.
C#
Copy
```
// An instance of  IUserInfoProvider (e.g., obtained via dependency injection)
private readonly IUserInfoProvider userInfoProvider;

// An instance of the object query
var query = userInfoProvider.Get();

// Gets all users and materializes the result
IEnumerable<User> users = query.GetEnumerableTypedResult();
```